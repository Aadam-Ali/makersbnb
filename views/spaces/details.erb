<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>MakersBnB</title>
  </head>
  <body>
  <nav class="nav nav-pills flex-column flex-sm-row">
    <a class="flex-sm-fill text-sm-center nav-link" aria-current="page" href="/spaces">Spaces</a>
    <a class="flex-sm-fill text-sm-center nav-link" href="/users/bookings">Bookings</a>
    <a class="flex-sm-fill text-sm-center nav-link" href="/incoming_bookings">Requests</a>
    <a class="flex-sm-fill text-sm-center nav-link" href="/login">Logout</a>
  </nav>
    <h3><%=@space.name %></h3>   
    <%= @space.description %>
    <br>
    <br>
    <h5>Price(£): </h5><%= @space.price %>
    <br>
    <br>
    <h5>Owner:</h5><%= @owner_name %>
    <br>
    <br>
    <h5>Available from:</h5> <%= @space.available_from %>
    <br>
    <br>
    <h5>Available to:</h5> <%= @space.available_to %>
    <br>
    <br>

    <form action="/bookings/new" method="post"> 
      <input type="hidden" name="property_id" value=<%= @space.id %>>
      <h5><label for="start">Choose date</label></h5>
      <input type="date" id="booking_date " name="booking_date" value=<%= @space.available_from %>  min=<%= @space.available_from %> max=<%= @space.available_to%>>
      <br>
      <br>
      <button type="submit" class="btn btn-primary">Book</button>
    </form>
 
    <!-- Option 1: Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
